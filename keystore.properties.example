# Release signing credentials for Anydown.
#
# Copy this file to keystore.properties (which is git-ignored) and fill in the
# real values. `storeFile` is relative to the app/ module directory.
storeFile=anydown-release.jks
storePassword=CHANGE_ME
keyAlias=anydown
keyPassword=CHANGE_ME
