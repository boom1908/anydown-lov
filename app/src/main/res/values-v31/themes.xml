<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Anydown" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/anydown_background</item>
        <!-- Android 12+ splash: logo mark on the app's dark canvas, no white box. -->
        <item name="android:windowSplashScreenBackground">@color/anydown_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_anydown_mark</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
    </style>
</resources>
